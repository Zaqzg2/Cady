# مراجعة شاملة: كادي (Flutter) ← خطة إعادة البناء الأصلي (Kotlin)

فحصت المشروع الكامل (113 ملف: pubspec + lib بالكامل + native/ + scripts/ + firestore.rules).
هذا يؤكد ويوسّع ما وصلني بالرفعة الأولى (نظام المزامنة/النسخ الاحتياطي كان
جزءًا صحيحًا ومطابقًا لما هنا). هذا المستند مرجعك لمراجعة التعديلات
والإضافات معي قبل ما نبدأ التنفيذ الفعلي، شاشة بشاشة.

---

## 1) جرد كامل للشاشات والوظائف

### الدخول والإعداد الأولي
- **SetupManagerScreen** — يظهر مرة واحدة بأول تشغيل، ينشئ أول حساب (مدير دائمًا)
- **LoginScreen** — يوزر/باسورد، رأس أحمر + لوحة بيضاء منحنية من الأعلى
- **LockScreen** — قفل PIN برقم حتى 8 خانات، لوحة أرقام كبيرة

### الشاشات الأساسية (مندوب) — `RootNav`: إعدادات(سحب)/رئيسية/عملاء/منتجات/تقارير
- **HomeScreen** — 4 بطاقات كبيرة (عميل نقدي، مرتجع، بيع، سند قبض) + "آخر العمليات"
- **CustomersScreen** — قائمة عملاء (بطاقة غنية: رصيد، حالة، إجراءات سريعة)
- **CustomerDetailScreen** (الأكبر، 872 سطر) — رأس + بطاقات مؤشرات + كشف حساب
  (معاينة/طباعة/مشاركة) + Timeline كامل لكل مستند مع قائمة إجراءات (معاينة/تعديل/طباعة/
  مشاركة/تحميل/نسخ رقم/تكرار/حذف) + اتصال/واتساب/فتح على الخريطة + تثبيت + ملاحظة
- **ProductsScreen** — بطاقات كبيرة، إضافة/تعديل/حذف (اسم، سعر، وحدة)
- **ReportsScreen** — ملخص فترة (مبيعات/مرتجعات/تحصيلات/صافي) + إجمالي مديونية كل العملاء
- **InvoiceScreen** — بيع/مرتجع، نقد/آجل، اختيار عميل ومنتجات، خصم %+مبلغ، توقيع،
  معاينة رصيد حي، حفظ/طباعة 80مم/PDF/مشاركة
- **ReceiptScreen** — نفس الفكرة لسند قبض (نقدًا/تحويل)، توقيع افتراضي محفوظ للمندوب
- **DocumentsListScreen** — سجل موحّد لكل الفواتير والسندات، فلاتر (الكل/بيع/مرتجع/سند)
- **PdfPreviewScreen** — معاينة PDF عامة قابلة لإعادة الاستخدام (طباعة/مشاركة من نفس الشاشة)

### الإعدادات — hub بخمس/ست شاشات فرعية
- **SettingsScreen** (hub) → الشركة، الطباعة، المظهر، الخصوصية، **المزامنة**، **النسخ الاحتياطي**، حجم البيانات
- **SettingsCompanyScreen** — شعار، بيانات شركة/مندوب، توقيع افتراضي، نص تذييل الفاتورة
- **SettingsPrintingScreen** (الأكبر بين شاشات الإعدادات) — طابعة بلوتوث + حالة اتصال
  حيّة حقيقية (لا مجرد افتراض محفوظ)، معاينة حية لحجم خط/تباعد الجدول والنص، عتبة
  تحويل رمادي←أسود، صيغة كشف الحساب الافتراضية (80مم/A4)، تبديل لحوار طباعة النظام
- **SettingsAppearanceScreen** — الوضع الليلي، حجم خط الواجهة، اهتزاز/صوت الحفظ، لون الهوية (7 خيارات)
- **SettingsPrivacyScreen** — كلمة مرور التطبيق، قفل تلقائي بعد خمول، **صف بصمة/وجه
  معطّل صراحة بتعليق "يحتاج تطوير إضافي"**، إخفاء الأرقام بالتطبيقات الأخيرة، خروج تلقائي
- **SettingsDataScreen** — حجم البيانات المخزّنة فعليًا (تقدير بايتات حقيقي)، تنظيف صور غير مستخدمة

### المزامنة والنسخ الاحتياطي (جانب المندوب)
- **SyncScreen** — بطاقة حالة (معلّق/متزامن)، قناتان (Firebase تلقائي + يدوي بلا إنترنت
  عبر مشاركة ملف JSON)، صادر/وارد، معاينة نسخة احتياطية، بانر تنبيه اسم الجهاز، نشاط حديث
- **SyncPendingPreviewScreen** — تفصيل كل سجل معلّق قبل التصدير
- **SyncOutboxInboxScreen** — سجل كل عمليات التصدير/الاستيراد السابقة (حتى 30، بإعادة إرسال)
- **BackupManagementScreen** — نسخ يدوي/تلقائي (يومي/أسبوعي)، تصدير/استيراد CSV، استرجاع
  من ملف خارجي أو من القائمة، مشاركة نسخة محفوظة، حذف

### لوحة المدير — `ManagerRootNav`: لوحة تحكم/مندوبون/مزامنة/نشاط مباشر
- **ManagerDashboardScreen** — عدد المندوبين والنشطين، بطاقة آخر نشاط مزامنة (من سجل حقيقي)
- **ManagerUsersScreen** (الأكبر بين شاشات المدير) — إدارة حسابات (إضافة/تعديل/إيقاف) +
  نشاط كل مندوب **الحقيقي من Firestore** (فواتير/سندات اليوم) — موثّق صراحة بالتعليقات
  أنه لا يعكس بيانات معلّقة محليًا لم تصل بعد
- **ManagerSyncHubScreen** — إحصاءات (أخطاء، عمليات اليوم، منتجات معلّقة، مندوبون نشطون)،
  حالة كل مندوب بالألوان حسب حداثة آخر مزامنة، استيراد/تصدير/سجل/نشاط مباشر
- **ManagerImportScreen** — استقبال ملف مندوب، معاينة كاملة قبل الاعتماد (تكرارات/أخطاء)،
  اعتماد ← يولّد ملف تأكيد استلام يُشارك رجوعًا للمندوب
- **ManagerExportScreen** — "إنشاء تحديث": منتجات/عملاء/إعدادات لمندوب واحد أو الكل
  (**صف "العروض" موجود ومعطّل بتعليق "غير متوفرة بالتطبيق حاليًا"**)
- **ManagerSyncLogScreen** — Timeline موحّد لكل عمليات الاستيراد والتصدير معًا
- **ManagerLiveActivityScreen** — بث حي (StreamBuilder) لكل فواتير/سندات كل المندوبين
  مباشرة من Firestore، بدون انتظار تصدير/استيراد يدوي، بنفس إجراءات شاشة العميل

---

## 2) الطبقة التقنية تحت الشاشات

**النماذج (12):** Customer, Product, Invoice(+Item), Receipt, CompanySettings(+ColumnConfig),
UserAccount, CustomerStats, DocRow (عرض موحّد فاتورة/سند), LedgerEntry (كشف حساب),
SyncStatus, SyncLogEntry, ExportLogEntry

**الخدمات — حسب الوظيفة:**
- بيانات: `DbService` (Hive + رفع Firestore أفضل-جهد + سحب/دمج last-write-wins)
- حسابات: `AccountService` (Firebase Auth + بريد وهمي من اسم المستخدم)، `AuthService` (PIN محلي SHA-256)
- ترقيم: `NumberingService` (يتتبع أعلى رقم مُستخدم فعليًا، يدعم تعديل يدوي بلا رجوع للخلف)
- طباعة/PDF: `PdfService`، `PrintService`/`PrintServiceIO`/`PrintServiceWeb`، `NativeBtBridge`
- مزامنة: `SyncService` (جانب مندوب)، `ManagerSyncService` (جانب مدير)، `ShareIntentService`
- نسخ احتياطي: `BackupService`، `AutoBackupService`، `CsvExportService`، `DataStatsService`، `CleanupService`
- مساعدة: `SettingsService`، `ImageStore` (صور كـ Base64 داخل Hive)، `ShareUtil`، `FeedbackService`، `Formatters`

**الحالة المركزية:** `AppProvider` (ChangeNotifier واحد يغطي كل شيء تقريبًا — عملاء،
منتجات، إعدادات، حسابات، مزامنة، فواتير/سندات).

**Native (موجود فعلًا بـ Kotlin):** `BluetoothPrinterBridge.kt`، `ShareIntentBridge.kt`
— كلاهما Kotlin نظيف لا يعتمد على أي مكتبة خارجية.

---

## 3) هذا مستوى جودة، لا مجرد "لا تحذفه" — احتفظ به بأمانة

بصراحة: لم أجد "أخطاء" تصميم أو بنية بالمعنى الحرفي. الكود موثّق بعناية غير
معتادة (كل قرار غير بديهي له تعليق يشرح **لماذا**، لا فقط ماذا). أبرز ما يستحق
أن يكون معيار الجودة للبناء الأصلي كله، لا فقط يُنسخ:

1. **محرك الطباعة الحرارية**: تشخيص حقيقي موثّق أن مكتبة خارجية كانت تفشل
   بالكتابة رغم نجاح الاتصال الظاهري → استبدال بـ Kotlin خام، كتابة مجزّأة على
   دفعات (تفادي فيض مخزن الطابعات الرخيصة)، تحقق حقيقي بكتابة فعلية لا `isConnected()`
   وحدها، إعادة اتصال تلقائية عند فشل الكتابة، سجل تشخيصي خطوة بخطوة قابل للنسخ،
   ومسار بديل عبر حوار طباعة النظام. تحويل صورة بعتبة ثابتة (لا Floyd-Steinberg)
   لأن النص يبدو أوضح هكذا على طابعة 203dpi — قرار مدروس بالتجربة الفعلية.
2. **بروتوكول المزامنة الثنائي القناة**: Firebase تلقائي + قناة يدوية بالكامل بلا
   إنترنت (مناسب تمامًا لواقع تغطية الشبكة بالميدان) — معاينة قبل اعتماد، دمج غير
   مدمِّر (upsert فقط)، ملفات تأكيد تُغلق الحلقة، سجلات outbox/inbox كاملة.
3. **نموذج الملكية والصلاحيات**: `firestore.rules` نظيف ومحكم (ownerUid يُقفَل
   عند أول حفظ، منتجات كتالوج مشترك، المدير يرى الكل).
4. **حساب الرصيد الآمن عند التعديل**: استبعاد المستند الحالي عند إعادة حساب
   رصيد المعاينة (`getCustomerBalanceExcluding`) — يمنع احتساب مزدوج.
5. **ترقيم المستندات**: يتتبع "أعلى رقم استُخدم فعليًا" لا عدّادًا بسيطًا — يدعم
   تعديل يدوي للرقم بلا كسر التسلسل لاحقًا.
6. **صدق واجهة المستخدم**: حالة "متصلة" بالطابعة، نشاط المندوبين، حالة المزامنة —
   كلها مبنية على بيانات حقيقية فعليًا، لا افتراضات متفائلة. هذا مبدأ تصميمي
   متكرر عبر التطبيق كله، وأقترح أن يبقى مبدأ التصميم الأول بالبناء الأصلي أيضًا.

---

## 4) قيود خاصة بـ Flutter/الويب فقط — تختفي أو تتحسّن تلقائيًا بالأصلي

هذه ليست أخطاء بالمشروع الحالي (قرارات معقولة ضمن قيود Flutter+الويب+CI عندهم)،
لكن لا داعي أبدًا لنقلها للبناء الأصلي:

- **كل مجلد `scripts/`** (9 سكربتات Python لترقيع `android/` بعد كل `flutter create`
  من الصفر بالـ CI) — يختفي بالكامل. بالأصلي، `android/` هو المشروع نفسه، كود
  عادي بالمستودع، بلا أي ترقيع بعد التوليد.
- **`ImageStore` كـ Base64 داخل Hive** بدل ملفات حقيقية — كان مقصودًا فقط
  ليتصرف مطابقًا على الويب (لا نظام ملفات حقيقي بالمتصفح). بالأصلي: ملفات
  حقيقية بمساحة تخزين التطبيق أرخص وأسرع (بلا تضخم Base64 ٣٣٪).
  bytes) → مجرد اقتراح تحسين طبيعي بعد سقوط قيد الويب.
- **كل فروع `kIsWeb`/`dart.library.io`** (`print_service_web.dart` وغيرها) — لا معنى لها بتطبيق أصلي بالكامل.
- **`AppProvider` الواحد الضخم**: يعمل تمامًا كما هو، لكن نمط Repository +
  ViewModel لكل شاشة (بدل ChangeNotifier واحد يغطي كل الدومين) أنسب معماريًا
  للأصلي — تطوير طبيعي لما هو ناجح فعلًا، لا إصلاح خلل.

---

## 5) إضافات مهمة — بعضها موثّق كفجوة معروفة بالكود نفسه

**موثّقة صراحة كفجوة بالمشروع الحالي (لا اقتراح مني — موجودة بالكود):**
- 🔓 **مصادقة بيومترية (بصمة/وجه)**: صف كامل بشاشة الخصوصية معطّل عمدًا،
  بتعليق صريح "يحتاج تطوير أندرويد إضافي، غير مفعّل بهذه النسخة". بالأصلي هذا
  عمليًا استدعاء AndroidX `BiometricPrompt` — من أسهل الإضافات وأعلى قيمة.
- 📦 **نسخ احتياطي تلقائي حقيقي بالخلفية**: تعليق صريح بـ `AutoBackupService`
  أن الفحص يحدث فقط عند فتح التطبيق "لأن التطبيق لا يعمل بالخلفية (لا خدمة
  نظام دائمة)". بالأصلي: `WorkManager` يعطي جدولة خلفية حقيقية حتى والتطبيق مغلق.
- 🏷️ **"العروض" الترويجية**: صف معطّل بشاشة تصدير المدير، بتعليق "الميزة غير
  متوفرة بالتطبيق حاليًا" — يبدو إنها كانت مخطط لها ولم تُبنَ بعد. **سؤال لك**:
  هل تريدها ضمن النسخة الأصلية الأولى، أم نؤجلها؟

**اقتراحاتي (بناءً على طبيعة الاستخدام الميداني اللي شفتها بالكود — مو موجودة حاليًا، تحتاج موافقتك):**
- ⚡ اختصارات تطبيق (App Shortcuts بالضغط المطوّل على الأيقونة): "فاتورة بيع
  جديدة" / "سند قبض" — امتداد طبيعي لفكرة بطاقات الرئيسية الأربع، يوفر خطوة كاملة.
- 📱 ودجة شاشة رئيسية (App Widget) بسيطة: إجمالي مبيعات اليوم + زر بيع سريع.
- 🖨️ حفظ أكثر من طابعة مقترنة (بدل واحدة فقط حاليًا) — مفيد لمندوب يتنقل بين أجهزة.

---

## 6) اتجاه UI/UX المقترح (أحدث بكثير من الحالي)

التصميم الحالي Material 3 نظيف ومتّسق (لمسات جيدة: بطاقات كبيرة مناسبة للمس،
معاينة حية بالإعدادات، مؤشرات حالة صادقة) لكنه "Material افتراضي" إلى حد كبير
بلا شخصية بصرية مميزة. مقترحي:

- نظام أشكال (shape system) مخصص بدل الزوايا الدائرية الموحدة بكل مكان
- Material 3 Expressive (حركة/انتقالات، مكوّنات التعبيرية الجديدة بأندرويد)
- تسلسل هرمي أقوى بالرئيسية (بطاقة بيع سريع بارزة، لا 4 بطاقات متساوية الحجم)
- استخدام الأحمر كهوية بجرأة أكبر في لحظات مختارة، لا فقط الأزرار
- طباعة/أيقونات مخصصة بدل Material الافتراضية بالكامل

هذا اتجاه أولي — أحب أعرف رأيك بمستوى الطموح قبل ما أثبّته بالتصميم الفعلي.

---

## 7) الأساس التقني المقترح للبناء الأصلي

Kotlin + Jetpack Compose + Material 3 + Navigation Compose + Room (بدل Hive) +
Hilt (DI) + WorkManager (نسخ احتياطي خلفية حقيقي) + DataStore (بدل SharedPreferences)
+ Firebase SDK الأصلي (Firestore/Auth) + AndroidX Biometric + نفس منطق
`BluetoothPrinterBridge.kt`/`ShareIntentBridge.kt` تقريبًا كما هو (كود Kotlin
عادي الآن، بلا أي MethodChannel).

مجموعة إصدارات Gradle/AGP/Kotlin/KSP/Hilt/Compose متحقق من توافقها فعليًا
عندي من عمل سابق بهذا المشروع تحديدًا — لن نعيد دورة تصحيح التوافق من الصفر.

---

## 8) قرارات مؤكدة

1. **العروض الترويجية**: ✅ ضمن النسخة الأولى.
2. **مستوى التصميم**: تحديث بصري ضمن نفس اللغة الحالية (Material 3 + الهوية
   الحمراء) — لا إعادة تصميم جذرية بالشكل/الحركة.
3. **الوصول للإعدادات**: أيقونة صريحة أعلى يمين الشاشة (إضافة على السحب، لا
   بديل عنه) — يمتد من نمط موجود جزئيًا بالرئيسية حاليًا (leading icon بالـ
   AppBar) ليصير متّسقًا بكل الشاشات المناسبة.
4. **الخط**: خط النظام الافتراضي بالجهاز — لا حزمة خط مخصص (لا Cairo ولا
   Tajawal). يقلل حجم APK ويحترم إعدادات حجم الخط بالنظام تلقائيًا.

## 9) تصحيحات إضافية من مراجعتك

### 🐛 مشكلة حقيقية: إعادة إنشاء حساب مدير بكل جهاز/مسح بيانات
شخّصت السبب بالكود: `AccountService.createUser()` لا يتحقق أبدًا من وجود
مدير مسبقًا قبل إنشاء حساب جديد — الحماية الوحيدة حاليًا نص تحذير بشاشة
الإعداد ("أنشئ حساب مدير جديد فقط لو ما عندك حساب من قبل") يعتمد كليًا على
قراءة المستخدم له، بلا أي تحقق برمجي فعلي خلفه. لو دخل المستخدم يوزر مختلف
عن حسابه الأصلي (نسيه، أو ما فهم إنه يحتاج فقط يسجّل دخول)، يتكوّن حساب
مدير جديد منفصل تمامًا وفارغ من كل البيانات القديمة.

**الحل بالبناء الأصلي**: شاشة الدخول تصير المسار الافتراضي الوحيد الواضح
دائمًا ("عندك حساب؟ سجّل دخول")، وخيار "إنشاء حساب مدير جديد" يصير مسارًا
ثانويًا متعمَّد الاحتكاك (تأكيد صريح إضافي: "أؤكد أنه لا يوجد حساب مدير
لهذا العمل من قبل") بدل رابط بضغطة واحدة كما هو الآن.

### 🎨 صفحة العميل: دمج إجراءات كشف الحساب بأعلى الصفحة
حاليًا `_buildStatementActions()` قسم منفصل بمنتصف الصفحة (بعد البطاقات
المؤشرات، قبل الـ Timeline). بالبناء الأصلي يصير جزءًا من رأس الصفحة
مباشرة (معاينة/طباعة كشف الحساب كأزرار بالرأس نفسه)، بدل قسم متكرر منفصل.

## 10) ترتيب البناء المقترح (حسب الاعتماد التقني)

**المرحلة ٠ — التأسيس**: إعداد Gradle (المجموعة المتحقق منها مسبقًا)، Hilt،
نظام التصميم/الثيم (خط النظام، الألوان)، هيكل التنقل، Room (كل النماذج
الـ12)، طبقة Repository

**المرحلة ١ — الهوية**: قفل PIN محلي، Firebase Auth، شاشة الدخول (بالتصحيح
أعلاه)، شاشة إعداد أول مدير (بالحل المذكور)

**المرحلة ٢ — الشاشات الأساسية**: الرئيسية، المنتجات، العملاء، صفحة العميل
(بالتعديل المذكور)

**المرحلة ٣ — المستندات**: الفاتورة، السند، توليد PDF، سجل المستندات، معاينة PDF

**المرحلة ٤ — الطباعة**: جسر البلوتوث (نقل شبه مباشر لـ
`BluetoothPrinterBridge.kt` الموجود فعلًا)، شاشة إعدادات الطباعة

**المرحلة ٥ — الإعدادات المتبقية**: الشركة، المظهر، الخصوصية (+البصمة
الجديدة)، حجم البيانات

**المرحلة ٦ — المزامنة والنسخ الاحتياطي**: شاشة المزامنة، النسخ الاحتياطي،
تصدير CSV، استقبال الملفات المشارَكة

**المرحلة ٧ — لوحة المدير**: التنقل، لوحة التحكم، المندوبون، مركز المزامنة،
الاستيراد/التصدير، السجل، النشاط المباشر

**المرحلة ٨ — الإضافات الجديدة**: العروض الترويجية، البصمة/الوجه، نسخ
احتياطي خلفي حقيقي (WorkManager)، اختصارات التطبيق

جاهز أبدأ بالمرحلة ٠ فورًا، أو تحب نناقش أي مرحلة أكثر أولًا؟
