# Room, Hilt, and Compose all ship consumer ProGuard rules inside their own
# AARs, so nothing manual is needed here yet. Add app-specific keep rules
# once isMinifyEnabled is flipped on for release builds.
